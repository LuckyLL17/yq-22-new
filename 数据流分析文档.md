# 餐厅匹配神器 - 完整数据流分析文档

## 目录
1. [入口分析
2. [状态管理分析
3. [匹配算法分析
4. [渲染流程分析
5. [完整数据流动图

---

## 1. 入口分析

### 1.1 应用入口

**入口文件**: [main.tsx](file:///Volumes/ExMac/traeProject/全站1/yq-22-new/src/main.tsx#L1-L10)

```tsx
createRoot(document.getElementById('root')!).render(
  <StrictMode>
    <App />
  </StrictMode>,
)
```

- 使用 React 18 的 `createRoot` API 挂载应用
- 包裹 `StrictMode` 用于开发环境检查
- 引入全局样式 `index.css`（Tailwind CSS

### 1.2 路由结构

**路由配置**: [App.tsx](file:///Volumes/ExMac/traeProject/全站1/yq-22-new/src/App.tsx#L1-L24)

```
/ (首页 - Home)
├── /history (历史记录)
├── /favorites (收藏)
├── /blacklist (黑名单)
├── /vote-manager (投票管理)
├── /vote/:voteId (投票参与页)
└── /share/:recordId (分享页面)
```

使用 `react-router-dom` 的 `BrowserRouter` 进行路由管理，共7个页面路由。

### 1.3 主页面 (Home)

**主页位置**: [Home.tsx](file:///Volumes/ExMac/traeProject/全站1/yq-22-new/src/pages/Home.tsx#L14-L391)

首页是整个应用的核心交互页面，包含两大视图状态切换：
- **收集模式：添加参与人员、设置匹配权重
- **结果模式：展示匹配结果、筛选排序、投票发起

---

## 2. 状态管理分析

### 2.1 状态库选型

使用 **Zustand** 作为状态管理库，配合 **persist** 中间件实现本地持久化存储。

**状态定义**: [useStore.ts](file:///Volumes/ExMac/traeProject/全站1/yq-22-new/src/store/useStore.ts#L6-L341)

### 2.2 核心状态结构

```typescript
interface StoreState {
  // 人员管理
  people: Person[];                      // 参与人员列表
  
  // 匹配结果
  matchResults: MatchResult[];            // 匹配结果列表
  isMatching: boolean;                  // 是否正在匹配中
  currentMatchStep: MatchStep;          // 当前匹配步骤（动画用）
  
  // 权重配置
  weights: WeightConfig;                // 匹配权重
  
  // 历史记录
  historyRecords: MatchRecord[];         // 历史匹配记录
  selectedHistoryIds: string[];
  
  // 收藏与黑名单
  favoriteRestaurantIds: string[];       // 收藏餐厅ID
  blacklistRestaurantIds: string[];    // 黑名单餐厅ID
  
  // 投票系统
  votes: Vote[];
  currentVoteId: string | null;
}
```

### 2.3 持久化配置

存储键名: `restaurant-match-storage`

持久化的状态字段：
- `historyRecords` - 历史记录
- `people` - 人员列表
- `matchResults` - 匹配结果
- `weights` - 权重配置
- `favoriteRestaurantIds` - 收藏
- `blacklistRestaurantIds` - 黑名单
- `votes` - 投票数据

### 2.4 核心 Action 分类

#### 人员管理 Actions
- `addPerson(person)` - 添加人员（自动生成ID和头像
- `removePerson(id)` - 删除人员
- `updatePerson(id, updates)` - 更新人员信息

#### 匹配 Actions
- `performMatch()` - 执行匹配（带动画步骤动画）
- `setCurrentMatchStep(step)` - 设置当前匹配步骤
- `clearResults()` - 清空结果
- `saveMatchRecord()` - 保存匹配记录到历史

#### 权重 Actions
- `updateWeight(key, value)` - 更新单项权重
- `resetWeights()` - 重置为默认权重

---

## 3. 匹配算法分析

### 3.1 算法入口

**算法文件**: [matchAlgorithm.ts](file:///Volumes/ExMac/traeProject/全站1/yq-22-new/src/utils/matchAlgorithm.ts#L1-L206)

**主函数**: `matchRestaurants(people, weights, favoriteIds, blacklistIds)

### 3.2 算法流程总览

```
输入: 人员列表 + 权重配置 + 收藏列表 + 黑名单
      ↓
1. 遍历所有餐厅（RESTAURANTS 数据源
      ↓
2. 黑名单过滤（跳过黑名单餐厅）
      ↓
3. 逐人计算分数（calculatePersonScore）
      ↓
4. 检查是否有0分（有则跳过该餐厅）
      ↓
5. 计算平均分 → 收藏加分
      ↓
6. 加入结果列表
      ↓
输出: 按匹配度降序排列的 MatchResult[]
```

### 3.3 单人分数计算 (calculatePersonScore)

**基础分**: 100 分

#### 硬性筛选（0分判定 - 直接淘汰）

1. **素食检查**
   - 条件：用户是素食者 && 餐厅无素食选项
   - 结果：直接返回 0 分
   - 原因：餐厅没有素食选项

2. **过敏源检查**
   - 条件：餐厅食材包含用户过敏源
   - 结果：直接返回 0 分
   - 原因：餐厅菜品含有过敏源：XXX

#### 扣分项目

3. **辣度差异惩罚**
   - 条件：餐厅辣度 > 用户可接受辣度
   - 公式：`惩罚 = (餐厅辣度 - 用户辣度) × spicyPenalty
   - 默认权重：spicyPenalty（默认25分/级）

4. **忌口食材惩罚**
   - 条件：餐厅食材包含用户忌口项
   - 公式：`惩罚 = 忌口数量 × dislikePenalty
   - 默认权重：dislikePenalty（默认20分/个）

#### 加分项目

5. **喜欢菜系加成**
   - 条件：餐厅菜系在用户喜欢列表中
   - 公式：`加分 = favoriteBonus
   - 默认权重：favoriteBonus（默认10分）

#### 分数范围限制

最终分数被限制在 `[0, 100] 范围内。

### 3.4 综合评分逻辑

1. 若有任何一人得0分 → 餐厅直接排除
2. 计算所有人的平均分
3. 若餐厅在收藏列表 → 额外加15分（FAVORITE_BONUS）
4. 最终分数限制在 [0, 100]

### 3.5 满意度判定

- **满意** (isSatisfied): 分数 ≥ 70 分
- **不满意** (isDissatisfied): 分数 < 70 分

### 3.6 结果排序

所有餐厅按 `matchScore` 从高到低排序。

---

## 4. 渲染流程分析

### 4.1 人员输入阶段

#### AddPersonForm 组件
**位置**: [AddPersonForm.tsx](file:///Volumes/ExMac/traeProject/全站1/yq-22-new/src/components/AddPersonForm.tsx#L6-L205)

**收集的信息**：
- 姓名 (name)
- 辣度接受程度 (spicyLevel: 0-3)
- 是否素食 (isVegetarian)
- 忌口食材 (dislikes)
- 过敏源 (allergies)
- 喜欢菜系 (favorites)

**数据流**：
```
用户填写表单
    ↓
本地 useState 管理表单状态
    ↓
点击确认添加
    ↓
调用 store.addPerson()
    ↓
生成唯一ID (Date.now().toString())
    ↓
随机生成头像 (AVATAR_EMOJIS)
    ↓
存入 people 数组
    ↓
触发重渲染 → PersonCard 列表更新
```

#### PersonCard 组件
**位置**: [PersonCard.tsx](file:///Volumes/ExMac/traeProject/全站1/yq-22-new/src/components/PersonCard.tsx#L10-L68)

展示人员的偏好标签：
- 辣度标签（橙色）
- 素食标签（绿色）
- 忌口标签（红色）
- 过敏标签（紫色）
- 喜欢菜系标签（蓝色）

### 4.2 权重调整阶段

#### WeightAdjuster 组件
**位置**: [WeightAdjuster.tsx](file:///Volumes/ExMac/traeProject/全站1/yq-22-new/src/components/WeightAdjuster.tsx#L24-L167)

**可调权重**：
- 辣度差异惩罚 (5-50分，步长5)
- 忌口食材惩罚 (5-40分，步长5)
- 喜欢菜系加成 (0-30分，步长5)

**数据流**：
```
用户拖动滑块
    ↓
调用 store.updateWeight(key, value)
    ↓
更新 weights 状态
    ↓
实时反映到下一次匹配时使用新权重
```

### 4.3 匹配执行阶段

#### 触发匹配
**位置**: [Home.tsx](file:///Volumes/ExMac/traeProject/全站1/yq-22-new/src/pages/Home.tsx#L81-L84)

```
点击"开始匹配餐厅"按钮
    ↓
调用 performMatch()
    ↓
设置 isMatching = true
    ↓
设置 currentMatchStep = 'collecting'
    ↓
启动步骤动画（每400ms切换一步）
    ↓
步骤：collecting → filtering → scoring → calculating → sorting → complete
    ↓
最后一步调用 matchRestaurants() 计算结果
    ↓
更新 matchResults
    ↓
设置 isMatching = false
    ↓
自动保存到历史记录
```

#### MatchingAnimation 组件
**位置**: [MatchingAnimation.tsx](file:///Volumes/ExMac/traeProject/全站1/yq-22-new/src/components/MatchingAnimation.tsx#L9-L121)

**6个匹配步骤：
1. 📋 收集偏好 - 整理所有人的饮食偏好
2. 🔍 筛选餐厅 - 排除不符合基本条件的餐厅
3. 📊 计算分数 - 为每个人计算匹配分数
4. ⚖️ 综合评估 - 计算团队整体匹配度
5. 🏆 排序结果 - 按匹配度从高到低排序
6. ✨ 匹配完成 - 为您找到最佳餐厅

### 4.4 结果展示阶段

#### 结果概览
- 匹配完成提示（渐变背景卡片）
- 分享按钮
- 筛选排序面板
- 快速发起投票
- 餐厅卡片列表

#### FilterSortPanel 组件
**位置**: [FilterSortPanel.tsx](file:///Volumes/ExMac/traeProject/全站1/yq-22-new/src/components/FilterSortPanel.tsx#L16-L316)

**筛选功能**：
- 排序方式：匹配度/评分/距离/价格
- 价格区间：最低/最高价格
- 距离范围：1/2/3/5/10公里
- 最低评分：4.0/4.5/4.8分
- 最低匹配度：60/70/80/90分

**筛选逻辑**：在 `Home.tsx` 的 `filteredAndSortedResults` useMemo 中计算

#### RestaurantCard 组件
**位置**: [RestaurantCard.tsx](file:///Volumes/ExMac/traeProject/全站1/yq-22-new/src/components/RestaurantCard.tsx#L211-L455)

**展示信息**：
- 餐厅图片、名称、评分、菜系、辣度
- 地址、价格、距离
- 满意人数 / 可能不满意人数
- 收藏/拉黑按钮
- 匹配分数 (MatchScore 组件)

**展开详情Tabs**：
1. 得分详情 - 每个人的详细得分
2. 菜品预览 - 招牌菜品
3. 相似推荐 - 相似口味餐厅
4. 替代方案 - 不同菜系替代

#### MatchScore 组件
**位置**: [MatchScore.tsx](file:///Volumes/ExMac/traeProject/全站1/yq-22-new/src/components/MatchScore.tsx#L7-L40)

分数颜色分级：
- ≥80分：绿色
- 60-79分：黄色
- <60分：红色

---

## 5. 完整数据流动图

### 5.1 总体数据流

```
┌─────────────────────────────────────────────────────────────────┐
│                        用户输入层                               │
└─────────────────────────────────────────────────────────────────┘
                              │
                              ▼
┌─────────────────────────────────────────────────────────────────┐
│  AddPersonForm 收集人员信息                                │
│  - 姓名、辣度、素食、忌口、过敏、喜欢菜系          │
└─────────────────────────────────────────────────────────────────┘
                              │
                              ▼
┌─────────────────────────────────────────────────────────────────┐
│  Zustand Store (useStore)                                  │
│  ┌─────────────────────────────────────────────────────┐ │
│  │  people: Person[]                                   │ │
│  │  weights: WeightConfig                          │ │
│  │  favoriteRestaurantIds: string[]              │ │
│  │  blacklistRestaurantIds: string[]            │ │
│  └─────────────────────────────────────────────────────┘ │
│         ↑ 持久化到 localStorage                      │
└─────────────────────────────────────────────────────────────────┘
                              │
                              ▼  点击"开始匹配餐厅"
┌─────────────────────────────────────────────────────────────────┐
│  performMatch() 执行匹配                                    │
│  ┌─────────────────────────────────────────────────────┐ │
│  │  1. 设置 isMatching = true                       │ │
│  │  2. 逐步更新 currentMatchStep (动画)        │ │
│  │  3. 调用 matchRestaurants() 算法            │ │
│  │  4. 更新 matchResults                       │ │
│  │  5. 自动保存到历史记录                    │ │
│  └─────────────────────────────────────────────────────┘ │
└─────────────────────────────────────────────────────────────────┘
                              │
                              ▼
┌─────────────────────────────────────────────────────────────────┐
│  matchRestaurants() 匹配算法                               │
│  ┌─────────────────────────────────────────────────────┐ │
│  │  输入: people, weights, favoriteIds, blacklistIds   │ │
│  │                                                   │ │
│  │  对每个餐厅:                                    │ │
│  │  ┌──────────────────────────────────────────┐  │ │
│  │  │ 1. 黑名单检查 → 跳过                  │  │ │
│  │  │ 2. 对每个人 calculatePersonScore()  │  │ │
│  │  │  │ 基础分 100                     │  │ │
│  │  │  │ 素食检查 (0分淘汰)             │  │ │
│  │  │  │ 过敏源检查 (0分淘汰)           │  │ │
│  │  │  │ 辣度差异惩罚                     │  │ │
│  │  │  │ 忌口食材惩罚                     │  │ │
│  │  │  │ 喜欢菜系加成                     │  │ │
│  │  │  └──────────────────────────────────────┘  │ │
│  │  3. 有0分 → 跳过餐厅                      │  │ │
│  │  4. 计算平均分                              │  │ │
│  │  5. 收藏餐厅 → +15分                        │  │ │
│  │  6. 加入结果列表                            │  │ │
│  │  └──────────────────────────────────────────┘  │ │
│  │                                                   │ │
│  │  输出: 按匹配度降序排列的 MatchResult[]        │ │
│  └─────────────────────────────────────────────────────┘ │
└─────────────────────────────────────────────────────────────────┘
                              │
                              ▼
┌─────────────────────────────────────────────────────────────────┐
│  结果渲染层                                                │
│  ┌─────────────────────────────────────────────────────┐ │
│  │  1. 匹配完成提示卡片                           │ │
│  │  2. 分享按钮                                    │ │
│  │  3. FilterSortPanel 筛选排序              │ │
│  │  4. 快速发起投票                                │ │
│  │  5. RestaurantCard 列表                    │ │
│  │     - 餐厅信息                                  │ │
│  │     - 匹配分数                                │ │
│  │     - 满意/不满意人数                     │ │
│  │     - 展开详情 (4个Tab)                │ │
│  └─────────────────────────────────────────────────────┘ │
└─────────────────────────────────────────────────────────────────┘
```

### 5.2 详细数据流动时序

#### 步骤1：人员输入
```
用户操作                 组件状态                Store状态              算法
  │                       │                     │                   │
  ├─ 填写姓名 ────────────► name=xxx          │                   │
  ├─ 选择辣度 ────────────► spicyLevel=1        │                   │
  ├─ 选择素食 ────────────► isVegetarian=false   │                   │
  ├─ 选择忌口 ────────────► dislikes=[...]      │                   │
  ├─ 选择过敏 ────────────► allergies=[...]     │                   │
  ├─ 选择菜系 ────────────► favorites=[...]    │                   │
  │                       │                     │                   │
  └─ 点击确认添加 ────────► handleSubmit()       │                   │
                            │                     │                   │
                            └── addPerson() ────► addPerson(person)   │
                                                  │                   │
                                                  ├─ 生成ID           │
                                                  ├─ 生成头像         │
                                                  └─ people.push()       │
```

#### 步骤2：匹配执行
```
用户操作                 组件状态                Store状态              算法
  │                       │                     │                   │
  └─ 点击开始匹配 ────────► handleMatch()       │                   │
                            │                     │                   │
                            └─ performMatch() ────► isMatching=true    │
                                                  currentMatchStep   │
                                                  ='collecting'      │
                                                  │                   │
                                                  ├─ setTimeout 300ms     │
                                                  │                   │
                                                  ├─ step 1: collecting │
                                                  │                   │
                                                  ├─ setTimeout 400ms     │
                                                  │                   │
                                                  ├─ step 2: filtering  │
                                                  │                   │
                                                  ├─ ... (共6步)     │
                                                  │                   │
                                                  └─ step 6: complete ───►
                                                                      │
                                                  matchRestaurants() ──────►
                                                  │                     │
                                                  ├─ 遍历餐厅          │
                                                  │  ├─ 黑名单检查     │
                                                  │  ├─ 逐人打分       │
                                                  │  ├─ 计算平均分     │
                                                  │  └─ 收藏加分       │
                                                  │                     │
                                                  ├─ 排序结果          │
                                                  │                     │
                                                  └─ 返回结果 ◄─────────┘
                                                        │
                                                  matchResults = results
                                                  isMatching = false
                                                  saveMatchRecord()
                                                        │
                                                  触发重渲染
```

#### 步骤3：结果展示与筛选
```
用户操作                 组件状态                Store状态              算法
  │                       │                     │                   │
  ├─ 查看结果 ◄─────────── 渲染 matchResults ◄─── matchResults       │
  │                       │                     │                   │
  ├─ 调整筛选 ────────────► filterConfig       │                   │
  │                       │  useMemo 计算        │                   │
  │                       │  filteredResults       │                   │
  │                       │                     │                   │
  ├─ 点击餐厅卡片 ◄────────── 渲染 RestaurantCard  │                   │
  │                       │  - 匹配分数      │                   │
  │                       │  - 满意人数      │                   │
  │                       │                     │                   │
  └─ 展开详情 ◄────────── 渲染 PersonScoreItem │
                          - 每人详细得分        │
                          - 扣分说明        │
```

### 5.3 关键数据结构流转

#### Person (人员
```
AddPersonForm (本地状态)
    │ 表单数据
    ▼
addPerson()
    │ 生成id、头像
    ▼
people: Person[]  (Store)
    │
    ├─ PersonCard 展示
    │
    ├─ performMatch() 时传入算法
    │
    └─ 保存到历史记录
```

#### MatchResult (匹配结果)
```
matchRestaurants() 算法
    │
    ▼
matchResults: MatchResult[]  (Store)
    │
    ├─ MatchingAnimation 过程展示
    │
    ├─ FilterSortPanel 筛选排序
    │
    ├─ RestaurantCard 列表展示
    │
    └─ 保存到历史记录 (MatchRecord)
```

#### MatchRecord (历史记录)
```
saveMatchRecord()
    │
    ▼
historyRecords: MatchRecord[]  (Store)
    │
    └─ History 页面展示
```

---

## 6. 核心文件索引

| 模块 | 文件 | 说明 |
|------|------|------|
| 入口 | [main.tsx](file:///Volumes/ExMac/traeProject/全站1/yq-22-new/src/main.tsx) | 应用入口 |
| 路由 | [App.tsx](file:///Volumes/ExMac/traeProject/全站1/yq-22-new/src/App.tsx) | 路由配置 |
| 状态 | [useStore.ts](file:///Volumes/ExMac/traeProject/全站1/yq-22-new/src/store/useStore.ts) | Zustand 状态管理 |
| 算法 | [matchAlgorithm.ts](file:///Volumes/ExMac/traeProject/全站1/yq-22-new/src/utils/matchAlgorithm.ts) | 匹配算法核心 |
| 类型 | [index.ts](file:///Volumes/ExMac/traeProject/全站1/yq-22-new/src/types/index.ts) | TypeScript 类型定义 |
| 数据 | [restaurants.ts](file:///Volumes/ExMac/traeProject/全站1/yq-22-new/src/data/restaurants.ts) | 餐厅模拟数据 |
| 主页 | [Home.tsx](file:///Volumes/ExMac/traeProject/全站1/yq-22-new/src/pages/Home.tsx) | 首页主组件 |
| 人员表单 | [AddPersonForm.tsx](file:///Volumes/ExMac/traeProject/全站1/yq-22-new/src/components/AddPersonForm.tsx) | 添加人员表单 |
| 人员卡片 | [PersonCard.tsx](file:///Volumes/ExMac/traeProject/全站1/yq-22-new/src/components/PersonCard.tsx) | 人员信息展示 |
| 权重调整 | [WeightAdjuster.tsx](file:///Volumes/ExMac/traeProject/全站1/yq-22-new/src/components/WeightAdjuster.tsx) | 匹配权重滑块 |
| 匹配动画 | [MatchingAnimation.tsx](file:///Volumes/ExMac/traeProject/全站1/yq-22-new/src/components/MatchingAnimation.tsx) | 匹配过程动画 |
| 餐厅卡片 | [RestaurantCard.tsx](file:///Volumes/ExMac/traeProject/全站1/yq-22-new/src/components/RestaurantCard.tsx) | 匹配结果卡片 |
| 匹配分数 | [MatchScore.tsx](file:///Volumes/ExMac/traeProject/全站1/yq-22-new/src/components/MatchScore.tsx) | 匹配度分数环 |
| 筛选排序 | [FilterSortPanel.tsx](file:///Volumes/ExMac/traeProject/全站1/yq-22-new/src/components/FilterSortPanel.tsx) | 结果筛选排序 |
| 历史记录 | [History.tsx](file:///Volumes/ExMac/traeProject/全站1/yq-22-new/src/pages/History.tsx) | 历史记录页面 |

---

## 7. 技术栈总结

- **框架**: React 18 + TypeScript
- **路由**: React Router v6
- **状态管理**: Zustand + persist 中间件
- **样式**: Tailwind CSS
- **UI 图标**: Lucide React
- **构建工具**: Vite
- **数据持久化**: LocalStorage
